#include<iostream>
using namespace std;
int main() {
   int num, i,ncount=0;
   cout<<"enter the number value:";
   cin>>num;
   cout << "The factors of " << num<< " are : ";
   for(i=1; i <= num; i++)
   ncount+=1 ;{
      if (num % i == 0)
      cout << i << ncount<<" ";
   }
   return 0;
}
